params {
    input = "${projectDir}/input.csv"
    num_lines = 2
    outdir = './results'    
}

profiles {
    docker {
        docker.enabled       = true
        docker.userEmulation = true
    }
}
