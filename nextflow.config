params {
    tower_host = 'https://tower.nf'
    tower_organisation = 'seqeralabs'
    tower_workspace = 'testing'   
}

profiles {
    docker {
        docker.enabled       = true
        docker.userEmulation = true
    }
}
